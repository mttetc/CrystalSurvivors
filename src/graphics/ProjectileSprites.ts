// All projectile textures are now loaded as PNG images in BootScene.preload().
// This file is kept as a no-op for backward compatibility.
export function generateProjectileTextures(_scene: Phaser.Scene): void {
  // No procedural textures needed — all replaced with Ninja Adventure assets
}
