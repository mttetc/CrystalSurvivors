// All UI textures are now loaded as PNG images from Ninja Adventure assets in BootScene.preload().
// This file is kept as a no-op for backward compatibility.
export function generateUITextures(_scene: Phaser.Scene): void {
  // No procedural textures — all replaced with Ninja Adventure skill icon assets
}

export function generateEnvironmentTextures(_scene: Phaser.Scene): void {
  // No procedural textures — all replaced with Ninja Adventure tileset assets
}
